### jitsi token step by step

```shell
$ 
$ sudo apt-get install libssl1.0-dev
$ sudo luarocks-5.2 install luacrypto
$ sudo luarocks-5.2 install basexx
$ sudo luarocks-5.2 install lbase64
$ sudo luarocks-5.2 install lua-cjson 2.1.0-1
$ sudo luarocks-5.2 install luajwtjitsi
$ apt-get install jitsi-meet-tokens
```
